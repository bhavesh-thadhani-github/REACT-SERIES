import InputBox from './InputBox'

export {InputBox}   //now we have to export only this file. We can also add other components here

//whichever components we make, we export them through a separate(here this-> index.js) file
